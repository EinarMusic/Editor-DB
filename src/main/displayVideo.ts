// const { app, BrowserWindow, nativeImage } = require('electron')
// const fs = require('fs')

// app.on('', () => {
//     fs.readFile('/Users/macbookpro2012/Desktop/test_one.mp4', (err: any, data: any) => {
//         if (err) throw err

//         const videoBlob = new Blob([data], { type: 'video/mp4' })
//         const videoUrl = URL.createObjectURL(videoBlob)

//         const videoImage = nativeImage.createFromBuffer(data)

//         const win = new BrowserWindow({
//             title: 'My Video',
//             width: 640,
//             height: 480,
//             icon: videoImage
//         })

//         win.loadURL(videoUrl)
//     })
// })

// mediaService.play('/Users/macbookpro2012/Desktop/test_one.mp4')







